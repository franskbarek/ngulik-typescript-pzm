export const sayHello = (name: String): String => {
  return `Hello, ${name}`;
};

export const sayGreating = (name: String, time: String): String => {
    return `Selamat ${time}, ${name}`;
  };
  

